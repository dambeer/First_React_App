import React from 'react';
import cl from './Loader.module.css';
const Loader = () => {
  return (
    <div
      style={{ margin: 'auto' }}
      className={cl.loader}></div>
  );
};

export default Loader;
